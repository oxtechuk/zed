export interface IContactInfo {
  whatsappNumber: string;
  whatsappHref: string;
  whatsappDisplay: string;
  phoneDisplay: string;
  phoneHref: string;
  emailDisplay: string;
  emailHref: string;
  addressDisplay: string;
}
